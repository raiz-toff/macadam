{
  "name": "macadam",
  "version": "1.0.0",
  "private": true,
  "description": "Macadam — Local-first gig driver earnings app",
  "scripts": {
    "dev": "node build.js --dev",
    "build": "node build.js --prod",
    "rebuild:provinces": "node scripts/rebuild-province-index.js",
    "preview": "npx --yes serve dist -s -l 3000"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  },
  "license": "MIT",
  "author": "raiz-toff",
  "repository": {
    "type": "git",
    "url": "https://github.com/raiz-toff/macadam.git"
  },
  "homepage": "https://github.com/raiz-toff/macadam",
  "keywords": [
    "gig-economy",
    "earnings-tracker",
    "pwa",
    "offline-first",
    "delivery-driver",
    "local-first"
  ]
}
