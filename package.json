{
  "name": "comma",
  "version": "1.3.0",
  "private": true,
  "description": "COMMA — Local-first gig driver earnings app",
  "scripts": {
    "dev": "node build.js --dev",
    "build": "node build.js --prod",
    "rebuild:provinces": "node scripts/rebuild-province-index.js",
    "preview": "npx --yes serve dist -s -l 3000"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  },
  "license": "MIT",
  "author": "raiz-toff",
  "repository": {
    "type": "git",
    "url": "https://github.com/raiz-toff/comma.git"
  },
  "homepage": "https://github.com/raiz-toff/comma",
  "keywords": [
    "gig-economy",
    "earnings-tracker",
    "pwa",
    "offline-first",
    "delivery-driver",
    "local-first"
  ]
}
