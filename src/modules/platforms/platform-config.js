/**
 * Platform-specific terminology (Feature 176 / locale-aware labels).
 * Populated by F11+; F7 `platformLabel` reads optional terms from here.
 * Shape: { [platformId]: { [termKey]: string } }
 */
export const PLATFORM_TERMINOLOGY = {};
