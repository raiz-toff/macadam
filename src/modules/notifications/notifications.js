/**
 * Notification types and on-open triggers (Phase 2). F5 boot calls a no-op until implemented.
 */

/** @returns {Promise<void>} */
export async function runOnOpenNotificationCheck() {
  return;
}
