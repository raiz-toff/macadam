# Macadam Web

Local-first gig earnings tracker (vanilla JS, Dexie, esbuild).

**Documentation:** see the [`docs/`](docs/) folder — start with [`docs/README.md`](docs/README.md).

**Build:** `node build.js --prod` (one-shot) · `node build.js` (watch mode for development).
